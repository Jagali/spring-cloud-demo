spring:
  application:
    name: service-ribbon

server:
  port: 8070

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
